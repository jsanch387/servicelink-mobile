/**
 * @typedef {'customer-search' | 'service-origin'} LocationAutocompleteMode
 */

/**
 * @typedef {{
 *   providerId: string;
 *   label: string;
 *   searchValue: string;
 *   city: string;
 *   state: string;
 *   zip: string;
 *   latitude: number;
 *   longitude: number;
 *   placeType: string;
 * }} StructuredLocation
 */

export {};
