/** Shared status pill sizing for maintenance list cards + detail (compact for scanning). */
export const MAINTENANCE_STATUS_PILL_LAYOUT = {
  borderRadius: 999,
  borderWidth: 1,
  flexShrink: 0,
  paddingHorizontal: 8,
  paddingVertical: 3,
};

export const MAINTENANCE_STATUS_PILL_TEXT = {
  fontSize: 11,
  fontWeight: '700',
  letterSpacing: -0.05,
};
