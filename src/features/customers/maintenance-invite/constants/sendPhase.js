/** @typedef {'wizard' | 'pending' | 'success' | 'error'} MaintenanceInviteSendPhase */

/** Minimum time to show the pending state so the transition is visible. */
export const MAINTENANCE_INVITE_SEND_MIN_PENDING_MS = 600;
