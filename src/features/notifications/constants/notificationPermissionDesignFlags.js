/**
 * Dev-only: pin the notification primer on Home while iterating on copy/layout.
 * Keep false in production — first-run gating lives in AuthNavigator.
 */
export const SHOW_NOTIFICATION_PERMISSION_DESIGN_PREVIEW = false;
