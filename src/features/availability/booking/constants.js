/** How far ahead the booking calendar allows selection (matches month picker default). */
export const BOOKING_CALENDAR_MAX_DAYS_AHEAD = 365;

/** Slot grid step in minutes (public web V2 booking). */
export const BOOKING_SLOT_INCREMENT_MINUTES = 30;
