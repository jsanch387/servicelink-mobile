/** Success animation hold before dismiss (mock path + success overlay). */
export const TAP_TO_PAY_PENDING_MS = 2100;

/** Brief pause on success before returning to the Complete sheet. */
export const TAP_TO_PAY_SUCCESS_DISMISS_MS = 1100;
